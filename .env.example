# Public frontend configuration only. Keep server secrets without a VITE_ prefix.
VITE_API_URL=

# Vercel / Neon PostgreSQL
DATABASE_URL=
SESSION_SECRET=

# Optional server-side Telegram bot token. Never expose it with VITE_.
SAT_TELEGRAM_BOT_TOKEN=
